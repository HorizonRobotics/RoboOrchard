# Enabled permits inference; it does not guarantee available observations/actions.
string ENABLED = "enabled"
string DISABLED = "disabled"

# Snapshot publication time, including periodic heartbeats.
std_msgs/Header header
string data
